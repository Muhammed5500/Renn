{
  "name": "@golge-defter/operator",
  "version": "0.1.0",
  "private": true,
  "description": "Shadow ledger (operator): accepts, orders and signs vouchers and sends batches to the chain. x402 facilitator and private entry relayer.",
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test \"test/*.test.ts\""
  },
  "dependencies": {
    "@golge-defter/sdk": "0.1.0",
    "@stellar/stellar-sdk": "^17.1.0"
  }
}
